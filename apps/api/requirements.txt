django>=5.2
djangorestframework>=3.15
djangorestframework-simplejwt>=5.3

# Optional realtime stack (WebSockets). Commented out to avoid Windows build issues
# when not needed. Enable later if you wire Channels/ASGI:
# channels>=4.1
# channels-redis>=4.2
# daphne>=4.1
# redis>=5.0

pytest>=8.0
pytest-django>=4.8
pytest-asyncio>=0.23
boto3>=1.34
django-cors-headers>=4.6
psycopg2-binary>=2.9
Pillow>=10.0
mercadopago>=2.2.5
qrcode[pil]>=7.4.2
